spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dbTest  # your dedicated test DB
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect